;;; init-windmove --- tweaks for windmove mode

;;; Commentary:

;;; Code:

;; windmove bindings clash badly with org mode, so disable them globally
(windmove-mode -1)

(provide 'init-windmove)
;;; init-windmove.el ends here
